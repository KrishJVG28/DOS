# Hybrid DoS Detection System

This project implements a hybrid DoS detection system using:

- Shannon Entropy (traffic randomness)
- Random Forest (classification)
- Secure logging (SHA-256 hashing)

## Features
- Detect Normal / Flash Crowd / DoS
- Simple ML model
- Secure log generation

## Run
```bash
pip install scikit-learn
python main.py
